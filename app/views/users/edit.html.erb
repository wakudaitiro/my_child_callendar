<% provide(:title, "Password Change") %>

<div class="main">
  <div class="devise-outer">
    <div class="form-wrap">
      <%= form_with model:@user, url: update_password_user_path, method: :patch do |f| %>
        <div class="form-group">
          <%= f.label :current_password, "現在のパスワード" %>
          <%= f.password_field :current_password, autofocus: true, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :password, "新しいパスワード" %>
          <%= f.password_field :password, autofocus: true, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :password_confirmation, "新しいパスワードの確認" %>
          <%= f.password_field :password_confirmation, autofocus: true, class: "form-control" %>
        </div>
        <div class="form-group text-center">
          <%= f.submit "変更する", class: "btn btn-primary" %>
        </div>
      <% end %>
      <br>
      <p><%= link_to "戻る", user_path(@user) %></p>
    </div>
  </div>
</div>
